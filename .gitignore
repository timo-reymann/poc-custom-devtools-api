.idea

# Dependencies (hoisted to the workspace root by npm workspaces)
node_modules

# Yarn local state (not meant to be committed without zero-installs)
.yarn/

# WXT extension build artifacts and generated types
extension/.wxt
extension/.output

# web-api build output
web-api/dist
